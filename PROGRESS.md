# LedgerOne — Build Progress
**Approach:** spec-first, per planning doc §11 order. Each spec is reviewed and approved before any code is scaffolded.
**Status (2026-07-06): All nine specs approved. Specs 01–05 are code-complete and UI-complete.** The user login/role-enforcement gap (previously flagged HIGHEST PRIORITY) is closed. **Spec 06 (Excel import/export) is the next build target — NOT yet started.**
**New to this project? Read the [README](README.md), then the "For a new developer joining this project" section at the bottom of this file, then [specs/00-key-decisions-digest.md](specs/00-key-decisions-digest.md).**
Planning doc: `C:\Users\Boyega\Downloads\SME_Finance_App_Planning_Document.md` · Stack: **Tauri + Rust + local SQLite** (agreed 2026-07-03).
Latest verified test run: **62/62 passing, 0 failed** (see "Verified test run" below).

## Spec status

| # | Spec | File | Status |
|---|---|---|---|
| 01 | Data model + posting engine | [specs/01-data-model-and-posting-engine.md](specs/01-data-model-and-posting-engine.md) | ✅ **Approved** (v0.2 — tax rules corrected to NTA 2025 / WHT Regs 2024: input VAT on all taxable inputs; `vat_exempt` ₦50M and `cit_exempt` ₦100M/₦250M as **separate independent flags**; ₦2M/TIN calendar-month WHT deduction exemption gated on `cit_exempt`) |
| 02 | COA seed + setup wizard | [specs/02-coa-seed-and-setup-wizard.md](specs/02-coa-seed-and-setup-wizard.md) | ✅ **Approved** (revised: tax flags **Advisor Mode only**, owner gets non-editable ledger-derived threshold banner; re-derivation reminder tied to `fiscal_year_start_month`) |
| 03 | Invoicing + payments | [specs/03-invoicing-and-payments.md](specs/03-invoicing-and-payments.md) | ✅ **Approved v1.1 — all decisions resolved** (NGN-only invoicing for v1; #9 resolved: zero-total invoices allowed for free samples/promo stock — COGS at WAC, zero revenue, confirm-warning; no JE when no inventory lines) |
| 04 | Expenses/bills + banking/reconciliation | [specs/04-expenses-bills-banking-reconciliation.md](specs/04-expenses-bills-banking-reconciliation.md) | ✅ **Approved v1.0 — all 10 decisions resolved** (#7 amended: write-off threshold + routing accounts are Advisor Mode company settings, seeded ₦5,000/6980/4200; #10: Spec 01 updated to v0.3 carrying the reconciliation `state` + matches-join-table shape as current-approved; #2–5/#8–9 approved via scope audit — none touch VAT/WHT posting or the balance guarantee) |
| 05 | Statements & reports | [specs/05-statements-and-reports.md](specs/05-statements-and-reports.md) | ✅ **Approved v1.0, and CODE-COMPLETE + UI-COMPLETE 2026-07-06** (spec decisions: #7 calendar-month VAT periods and #8 computed credit carry-forward individually confirmed; no-closing-entries model + terminology guard [period *lock* ≠ closing *entries*]; live-query discipline extended to WHT cumulative credit — see "Build status" below for the implementation) |
| 06 | Excel import/export | [specs/06-excel-import-export.md](specs/06-excel-import-export.md) | ✅ **Approved v1.0** (#2 confirmed with rationale on record: historical open invoices post Dr AR / Cr OBE — never revenue/VAT — to avoid restating filed income or creating phantom VAT liability; rest batch-approved via scope audit) |
| 07 | Dashboard + Owner/Advisor Mode UX | [specs/07-dashboard-and-modes-ux.md](specs/07-dashboard-and-modes-ux.md) | ✅ **Approved v1.0** (#8 unremitted-tax line confirmed with rationale on record; §5 capability table endorsed as the single normative mode-gating source, superseding scattered wording in Specs 01–06) |
| 08 | Backups | [specs/08-backups.md](specs/08-backups.md) | ✅ **Approved v1.0** (batch; reviewer named as right calls: forever-kept fiscal-year-end snapshots, never-destroy restore posture, manifest outside the db) |
| 09 | Google Sheets push | [specs/09-google-sheets-push.md](specs/09-google-sheets-push.md) | ✅ **Approved v1.0** (#1 single-computation-path and #4 tokens-in-OS-store individually confirmed; #5 amended: **Sales by Customer tab added** — advisor's monthly customer-concentration diagnostic. Spec now, build Phase 2) |
| — | **Key-decisions digest** | [specs/00-key-decisions-digest.md](specs/00-key-decisions-digest.md) | ✅ **Filed** — one-page consolidation of all nine specs; the project's orientation document and CLAUDE.md seed |

## Open items
1. **▶ Working mode (per user 2026-07-04): autonomous to completion — no feedback pauses.** **Specs 01–05 are now UI-complete** (login/role enforcement wired 2026-07-06; Spec 05 reports built the same day — see "Build status"). Remaining build order: **Spec 06 Excel I/O** (next) → Spec 07 Advisor Mode + dashboard polish (capability gating in UI now has a real session/elevation layer to sit on, and Trial Balance/GL already prove the pattern out) → Spec 08 backups → packaging (NSIS installer, signing once CAC details arrive, updater keys, x64 cross-target). Sheets push stays Phase 2 by design.
2. **Known gaps to close during the remaining stack** (spec'd but not built; engine support exists where noted):
   - ✅ ~~HIGHEST PRIORITY — user attribution & role enforcement~~ **RESOLVED 2026-07-06.** See "User login & Advisor Mode elevation" under Build status for the full description.
   - ⏳ **Write-off amount limit — OPEN BUSINESS DECISION, not a build task.** The write-off command is role-gated (staff blocked), but whether/how an Advisor-Mode-elevated session bypasses `writeoff_limit_kobo` is a decision the user still needs to make — it changes engine behavior, not just command-layer gating. Recorded as **Decision #11 in [specs/04-expenses-bills-banking-reconciliation.md](specs/04-expenses-bills-banking-reconciliation.md) §7.5/§9** (the authoritative record — do not decide this here). Interim behavior: `recon::write_off` refuses unconditionally above the limit for every role. `require_advisor_elevated` (the guard the eventual bypass would use) is already implemented and tested in `ledger_core::auth`, waiting on the decision.
   - Products/price-list + inventory-tracking UI — engine COGS-at-WAC path and wizard opening-stock support exist and are tested, but invoice lines are free-description only, so the inventory engine is currently unreachable from the UI.
   - Soft-close confirm-retry flow — engine returns the `soft_close` error and screens display it, but all screens pass `confirm_soft_close:false` and none offer the "confirm and record anyway" retry button (Spec 01 P4).
   - Deposit apply/refund screens — `apply_deposit`/`refund_deposit`/`deposit_balance` are implemented and tested in ledger-core; no UI.
   - FX-receipt fields — `post_payment_in` accepts `FxReceipt` (realized gain/loss tested); payment form has no FX inputs.
   - Recurring templates UI — `recurring_templates` table exists; no reminder/draft flow (Spec 04 §4).
   - Bank-import mapping-profile persistence — `bank_import_profiles` table exists; the reconcile screen re-asks the mapping on every import.
   - Multi-company switcher — first company auto-selected; advisors managing several clients can't switch.
   - Supplier TIN save from the inline picker — needs an `update_contact` command; until then the engine treats missing TIN conservatively (explicit 2× WHT decision, never silent).
   - Contact statement of account (Spec 05 §6) — `reports::contact_statement` is implemented and tested; no UI button calls it yet (the natural spot is a "Statement" link from a customer/supplier row in Sales/Purchases).
   - Read-only commands (dashboard, list_invoices, list_bills, recon_state, etc.) remain reachable without an active session — only *mutating* commands were gated in the 2026-07-06 auth work. A full "no session ⇒ no access at all" lockdown, if wanted, is a small additional pass over the same `require_session()` pattern.
   - A handful of mutating commands (`create_contact`, `add_bank_account`, `log_delivery`, `recon_start/import_csv/match/unmatch/complete`) now require a session but don't attribute a `created_by` anywhere, because `contacts`, `bank_accounts`, and `reconciliations` have no such column in the schema — adding one is a migration, out of this task's "wire what exists" scope.
3. ✅ **Design decision — CONFIRMED 2026-07-06, not a compromise:** base session login is a PIN-less user picker (attribution only); the PIN is asked for exactly once, at Advisor Mode elevation, exactly as Spec 07 §5 describes ("not a separate login"). The alternative — prompting for the owner's PIN at ordinary sign-in — would have made that PIN do double duty as both a login credential and an elevation secret, contradicting Spec 07. Reviewer confirmed this split as the correct design.
4. **Live-run validation (2026-07-04):** EULA gate → all 8 wizard steps → atomic create → dashboard verified on real WebView2 via desktop automation. Findings: rendering clean (cards, chips, disabled-state gating all correct); wizard validation errors render in owner language (PIN mismatch case seen live); IPC felt instant at empty-company scale; the one visual nit (Step-1 wrapped-label baseline) is fixed. Caveats: the session collided with a human user at the console (window closes + stray typing — future live tests should be scheduled); a Tauri window was once found squashed to 13×13 px despite `minWidth`/`minHeight` — one-off, not reproduced, watch for recurrence. Dashboard tiles populate from live queries against wizard output (cash per account + total confirmed; AR/AP/profit tiles render — deep verification of those numbers comes with real transactions).
5. ⚠️ **LEGAL — EULA is drafted, NOT lawyer-reviewed** ([docs/legal/EULA.md](docs/legal/EULA.md), version `EULA-DRAFT-1`): **do not distribute outside internal/EdenOceans use until reviewed.** Counsel checklist is at the bottom of the file (liability cap, NDPA notice interaction, FCCPA carve-outs, entity naming vs CAC/certificate).
6. **Spec 02 decision #9** — WHT preset rates (§4.2 seed data) still need confirmation against the current WHT Regulations 2024 schedule before ship (data, not structure).
7. **Spec 01 §6.2 note** — WHT exemption gated on `cit_exempt` is an interpretive call (WHT Regs "small company" read as the CIT definition); confirm against FIRS guidance with the practice.

## Verified test run (2026-07-06, `cargo test --workspace`, literal output)
```
ledger_core (unit)        5 passed
tests/auth.rs             6 passed
tests/engine.rs          24 passed
tests/recon.rs            2 passed
tests/reports.rs         13 passed
tests/triggers.rs        12 passed
ledgerone_desktop_lib     0 tests
ledgerone_desktop (main)  0 tests
doc-tests (x2)            0 tests
TOTAL: 62 passed; 0 failed; 0 ignored
```
Three non-blocking `unused_mut` / `unused_import` warnings remain (seed.rs:330, recon.rs test:6, triggers.rs:41/128) — cosmetic, left for the next dev to sweep with `cargo fix`.

## Build status (updated 2026-07-06)
- ✅ **Spec 05 — Statements & Reports, engine + shell + UI, all in this session.** True to the spec's "no schema, no engine surface" (§7), everything lives in a new `crates/ledger-core/src/reports.rs` — pure read-only queries, zero posting, zero new tables. Covers every section of the spec: owner-tier (aging AR/AP with buckets and deposits shown separately/never netted; sales by customer and by product with top-N + rollup; drawings-in-range for the profit note line), formal statements (accrual Income Statement with gross/operating/net profit and margin %; the cash-basis P&L toggle implementing the exact §4.2 allocation-proportional algorithm — integer-only `round_ratio`, no floats near money; Balance Sheet with computed Retained/Current-Year Earnings per the no-closing-entries model, always ties by construction; indirect Cash Flow Statement), Advisor Mode-only Trial Balance + General Ledger (**the first real UI consumer of `require_advisor_elevated`**, closing the loop the login work opened — not shown to staff at all, shows a plain-language nudge rather than an error when role-eligible but not yet elevated), tax reports (VAT report netting output/input with live credit-carry-forward; WHT remittance + credit schedules; cumulative WHT credit as a live query), and a contact statement of account (engine + command built and tested; **not yet wired to a UI button** — noted below as the one loose end). **13 new tests in `tests/reports.rs`**, including two designed as the highest-stakes proofs the spec asks for: `balance_sheet_ties_across_a_fiscal_year_boundary` (a multi-transaction scenario spanning a fiscal-year rollover, with a manual advisor adjustment to Retained Earnings, asserting Assets = Liabilities + Equity exactly) and `cash_basis_recognizes_proportionally_at_payment_and_application_date` (hand-verifies the §4.2 formula against `money::round_ratio` directly). One real bug caught and fixed by the tests before commit: the accrual Income Statement's OpEx/COGS figures were coming out negative (a helper meant to flip income's credit-normal sign was applied uniformly instead of only to income, not cogs/expense) — the test suite caught it immediately, not a shipped defect.
- ✅ **User login & Advisor Mode elevation** (closes the previously-HIGHEST-PRIORITY gap): new `ledger_core::auth` module — `SessionStore` (in-memory, one signed-in user per process), `login`/`logout`/`current`, `require_session`/`require_not_staff` (the command-layer guards), `advisor_enter`/`advisor_exit`/`advisor_active` (PIN elevation per Spec 07 §5: argon2-verified against the owner's PIN, 15-min idle auto-exit measured against the company's `advisor_timeout_minutes`, 5-failed-attempt lockout, every transition audit-logged as `mode.entered`/`mode.pin_failed`/`mode.lockout`/`mode.exited`/`mode.timeout`). `hash_pin()` extracted from the wizard's inline argon2 call so there's one hashing path. **6 new tests in `tests/auth.rs`**, proving exactly the three things asked for: an unauthenticated session can't post (checked before any engine call), a posting's `created_by` matches the real logged-in user (including across a user switch), and a staff-role session is rejected by `require_not_staff` before `void_invoice`/`void_payment` ever run — the invoice is provably untouched afterward. Shell: every mutating command now builds its `PostCtx` from the active session (`ctx()`/`ctx_not_staff()` helpers) instead of `user_id: None`; `void_invoice_cmd`, `void_payment_cmd`, `recon_import_error` are role-gated (owner/advisor only, Spec 02 matrix); `recon_flag` stays open to every role per Spec 04 §7.5 but now attributes to the real user; `recon_writeoff` is role-gated (the amount-limit-bypass-via-elevation nuance is NOT wired — see Open Items #2). UI: a "Who's using LedgerOne?" picker at launch (PIN-less — see Open Items #3 for why), a persistent session strip on every screen with an unmistakable "🔓 ADVISOR MODE" badge when elevated, and inline Enter/Exit Advisor Mode controls hidden entirely for staff. The wizard auto-logs-in the new owner the moment setup completes.
- ✅ **Engine gaps closed** (commit abc7a24): `post_expense` (VAT back-out + WHT), deposit lifecycle (`apply_deposit`/`refund_deposit`, live-query balance, status recompute reads deposit applications from the ledger), `void_invoice` (payment guard, stock restored at original cost), FX receipts (realized gain/loss → 4900). Migration 0002 (`license_key`) proves the multi-migration path. **38/38 tests.**
- ✅ **Tauri shell compiling and committed** (139ef57): `src-tauri` in workspace; commands `create_company` / `list_companies` / `add_bank_account` / `dashboard` / `record_drawing`; **CmdError envelope** = owner-language message + next step, technical detail reserved for Advisor Mode — the client-facing-errors rule is structural from the first command. Capability: core IPC only. Node.js LTS (ARM64) installed.
- ✅ **Spec 04 back half — reconciliation** (05be684): `recon.rs` in ledger-core (CSV parse with mapping, SHA-256 dedup + occurrence index, auto-match only on unique candidates, 1:N sum-exact manual match, **needs-review = workflow quarantine that posts nothing**, write-off with routing settings + threshold gate, carry-forward, `completed_with_exceptions`, P6 lock stamp — all tested, 43/43); 11 commands; full reconcile screen under Bank & Cash (owner-language equation strip, inline actions with mandatory notes).
- ✅ **Spec 04 UI + Spec 03 completion** (e7c8cf4): engine `convert_quote`/`void_payment` (41/41 tests); 14 new commands; UI now covers Purchases (bills, pay-supplier with FIFO allocations and the **on-screen no-TIN WHT decision flow** — engine refuses to decide silently, screen offers manual amount or no-deduction), Record Expense (VAT back-out, collapsed WHT), Bank & Cash (Move Money, owner in/out), Payments register with voids, quotes (never post, one-click convert), printable invoice/quote document (@media print → system Save-as-PDF) and wa.me WhatsApp share with delivery logging. Opener plugin added (capability: core + opener only).
- ✅ **Invoicing + payments UI** (e154b2b): 8 new commands (contacts with dedupe-nudge inline create, invoice draft/send, register with derived overdue chips, open-invoices FIFO, payment-in, banks); frontend: invoice form with live estimate totals (engine figures normative at posting), payment form with FIFO auto-allocation + collapsed WHT + live "kept as deposit" remainder. Awaiting user's manual click-through.
- ✅ **Wizard end-to-end + EULA gate** (41fa910): `seed::create_company_full` — the whole Spec 02 wizard in ONE transaction (company, COA, banks, contacts, opening JE with OBE plug, users with argon2 advisor PIN, `eula.accepted` audit row; refusal creates zero rows — tested). `ui/` now carries the real flow: EULA click-through → 8 owner-language steps (three tax questions derive `vat_exempt`/`cit_exempt` independently with live plain-language consequences incl. the >₦50M-unregistered warning) → atomic **Create my books** → dashboard. **39/39 tests.**
- ✅ **Workspace + schema**: `crates/ledger-core`; all 27 tables + T1–T8 triggers ([0001_initial.sql](crates/ledger-core/migrations/0001_initial.sql)); migration runner.
- ✅ **Posting engine complete (Spec 01 §6)**: `post_journal`, `create/post_invoice` (COGS at WAC, P7 stock guard, zero-total free samples), `create/post_bill` (claimable vs absorbed input VAT per NTA 2025), `post_payment_in` (allocations, customer-withheld WHT, overpayment→deposit, RCT numbers), `post_payment_out` (WHT split at payment; ₦2M/TIN calendar-month exemption gated on `cit_exempt`; no-TIN → explicit 2× decision, never silent), `post_transfer` (fee → 6900, B1 cash floor), `post_drawing`/capital, `void_entry` (cross-linked negated reversal). Plus `seed::create_company` (atomic W1, 53-account COA, sequences, WHT presets, write-off routing) and centralized half-away-from-zero rounding (`money.rs`).
- ✅ **34/34 tests passing**: 12 trigger tests (incl. raw-SQL bypass attacks), 17 engine template tests asserting **exact Dr/Cr line sets** per Spec 01 §6 table + the tax edge ladder (exemption applies / lost above ₦2M aggregate / no-TIN decision required), 5 unit tests.
- **Environment notes**: machine is **Windows ARM64** (`aarch64-pc-windows-msvc`, Rust 1.96.1). VS Build Tools 2022 installed via winget — the ARM64 MSVC component (`VC.Tools.ARM64`) had to be added separately (the VCTools workload defaults assume x64 host); elevation for VS installer modifications must go through winget (`--force --override`), as direct `setup.exe --quiet` is blocked from non-elevated shells. `cargo` lives at `%USERPROFILE%\.cargo\bin` (PATH refresh needed in preexisting shells). **Node.js not yet installed** — needed when the Tauri shell is scaffolded; get the ARM64 build. `chrono` was dropped from ledger-core (autocfg build-script failure in this environment); timestamps are std-only civil-date math in `ids.rs`.
- **Known build annoyance**: intermittent `LNK1104`/`LNK1201` (cannot open exe/pdb) on rebuilds — likely Defender scanning fresh binaries in `target\debug\deps`. Workaround that works: delete `target\debug\deps\*.exe` **and** `*.pdb`, rebuild. Durable fix (user action, needs admin): add the project `target` folder to Windows Defender exclusions. Disk note: ~20 GB free on C: — watch it; `cargo clean` reclaims several GB if needed.

## Standing design rules (from reviews)
- Double-entry rigor under the hood, owner-friendly surface; nothing posted is ever deleted (void/reverse only).
- Compliance-critical settings: **advisor-gated edits, owner-facing visibility** (banners derived from ledger data) — never owner edit power.
- Tax rates/thresholds are **data, not code**; reminders follow the fiscal year, not the calendar year.

---

## For a new developer joining this project

You are inheriting a **spec-first** codebase: every feature was written as a reviewed markdown spec in `specs/` **before** any code. When in doubt about intended behaviour, the spec is the source of truth, and [specs/00-key-decisions-digest.md](specs/00-key-decisions-digest.md) is the one-page map to all of it. This file (PROGRESS.md) is the authoritative *status* doc — what is built, what is not, what to do next.

### Build & run from a clean clone
Prerequisites: **Rust** (stable, MSVC toolchain), **Node.js** (only needed if you regenerate Tauri assets; the committed `ui/` is plain HTML/JS and needs no build step). No database setup — SQLite is embedded (`rusqlite` `bundled` feature) and the schema is created on first run by the migration runner in `crates/ledger-core/src/db.rs`.

```
cargo test --workspace          # run the whole suite (currently 62 tests)
cargo build -p ledgerone-desktop   # build the desktop app
cargo run   -p ledgerone-desktop   # launch it (creates its DB under %APPDATA%\ng.boltonadvisory.ledgerone)
```
The running app's data lives at `%APPDATA%\ng.boltonadvisory.ledgerone\ledgerone.db`. To reset to a first-run state, rename/move that file (do not delete blindly — it is real books once used).

### Windows / ARM64 toolchain gotchas (this dev machine is Windows on ARM64)
These bit the previous developer; they will bite you too on a similar machine:
- **`VC.Tools.ARM64` MSVC component**: the VS 2022 "VCTools" workload installs x64-host tools by default. On an ARM64 host you must add the ARM64 component separately or linking fails with "link.exe not found". Install via winget with `--force --override "... --add Microsoft.VisualStudio.Component.VC.Tools.ARM64"`; the VS installer's own `setup.exe --quiet` is blocked from a non-elevated shell (exit 5007) — go through winget.
- **rustup `settings.toml` conflict**: if a stale `~/.rustup/settings.toml` points at a toolchain that no longer exists, `cargo` errors before it does anything. Fix by reinstalling the stable toolchain (`rustup toolchain install stable-aarch64-pc-windows-msvc`) or clearing the stale default.
- **`cargo` on PATH**: lives at `%USERPROFILE%\.cargo\bin`. Pre-existing shells may not have it until PATH is refreshed; call it by full path if `cargo` is "not recognized".
- **Windows Defender + `target/`**: Defender scans freshly-linked binaries and intermittently holds a lock, causing `LNK1104`/`LNK1201`. **Add the project `target/` folder to Defender exclusions** (needs admin) — this is the durable fix. Stop-gap without admin: `Remove-Item target\debug\deps\*.exe, target\debug\deps\*.pdb` then rebuild.
- **`chrono` is banned from `ledger-core`**: its `num-traits`/`autocfg` build script fails in this environment. Timestamps are hand-rolled std-only civil-date math in `ids.rs` — do not reintroduce chrono.

### Architectural boundary — do not cross it
This is the single most important rule in the codebase:

> **All posting/business logic lives in `crates/ledger-core`. The Tauri shell (`src-tauri/src/lib.rs`) contains only thin command wrappers that (de)serialize DTOs and call `ledger-core`. Never write ledger logic, SQL that posts, or accounting rules in the shell.**

- Every financial event flows through one posting surface (`engine.rs` functions → `posting::post_entry_in`) inside one DB transaction; the balance guarantee is enforced by SQLite triggers (Spec 01 §4), not by app code. See [specs/00-key-decisions-digest.md](specs/00-key-decisions-digest.md) "five invariants."
- Errors returned to the UI use the `CmdError` envelope: an owner-language message + a next step, with any technical detail carried separately for Advisor Mode. Every new command must keep this — no raw engine strings reach the owner.
- The frontend (`ui/index.html`) is a single dependency-free HTML/JS file talking to commands via `window.__TAURI__.core.invoke`. Its only capabilities are core IPC + the opener plugin (wa.me/mailto). No shell-exec, no fs, no arbitrary network.

### ✅ User attribution, session, and Advisor Mode elevation — RESOLVED 2026-07-06
This used to be the highest-priority gap ("Spec 07's Advisor Mode design assumes this exists — build it before starting Spec 07"). It's built now: `ledger_core::auth` (`SessionStore`, `login`/`logout`, `require_session`/`require_not_staff`, `advisor_enter`/`advisor_exit`/`advisor_active`), a user-picker screen at launch, a persistent session strip with an Advisor Mode badge, and role gating at the command layer (not just hidden buttons) on voids and reconciliation write-off/import-error. Every mutating command now attributes to a real `user_id`. Full description in PROGRESS.md's "Build status" section; tests in `crates/ledger-core/tests/auth.rs`. `require_advisor_elevated` now has its **first real UI consumer**: Spec 05's Trial Balance / General Ledger screen (Reports → Advisor tab) — staff never sees it, and an eligible-but-not-yet-elevated owner/advisor sees a plain-language nudge to elevate rather than an error. **Spec 07's own capability-gated screens (tax-flag settings, manual journals, hard period lock) can build on the same guard.** The write-off amount limit's Advisor-Mode bypass is intentionally *not* built — it's an open business decision (Spec 04 §9 Decision #11), not a wiring gap; see "Known gaps" #2 above.

### Where to start
Spec 05 (Statements & Reports) is now code-complete and UI-complete — see "Build status" above. The next build target is **Spec 06 — Excel Import/Export** ([specs/06-excel-import-export.md](specs/06-excel-import-export.md)), fully approved and unstarted: onboarding import (contacts, products, opening balances), bank-statement import already exists (Spec 04) but its *mapping-profile persistence* doesn't (a Spec 06-adjacent small gap, see "Known gaps"), and full-fidelity `.xlsx` export of every report built in Spec 05. Keep the same boundary: parsing/validation/mapping logic belongs in `ledger-core` with tests; the shell stays a thin wrapper.
