{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core IPC only. No shell, fs, or network capabilities — the frontend talks to ledger-core through commands and nothing else.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
